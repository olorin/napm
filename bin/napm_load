#! /bin/bash

# Load a password from napm into the X clipboard for the specified context.

if [[ $# -lt 1 ]]; then
	echo "napm_load: load a password from napm to the X clipboard."
	echo "Usage: $0 <context>"
	exit 1
fi

napm -c $1 | xclip -n
